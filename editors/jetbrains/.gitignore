.gradle/
build/
*.zip
# The Gradle wrapper jar is generated by `gradle wrapper` (see README);
# the wrapper scripts/properties are committed, the jar is not checked
# in here because it can't be produced without a local Gradle.
gradle/wrapper/gradle-wrapper.jar
