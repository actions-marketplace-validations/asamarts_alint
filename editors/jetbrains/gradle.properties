# Stable Gradle config for the IntelliJ Platform plugin build.
# (Configuration cache is left at the default off — the IntelliJ
# Platform Gradle plugin's config-cache support is version-sensitive.)
org.gradle.caching=true
org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC
kotlin.stdlib.default.dependency=false
