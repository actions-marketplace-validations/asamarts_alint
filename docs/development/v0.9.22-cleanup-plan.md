# alint v0.9.22 cleanup plan

> **Status**: locked-in plan; Batch A in progress as of 2026-05-14.
> **Source audit**: comprehensive audit on 2026-05-14 of alint + alint.org repos.
> **Decisions captured**: 2026-05-14.

A doc + prevention-automation cycle following the v0.9.21 release. The audit surfaced 10 categories of drift between the alint repo and the alint.org repo. This document is the agreed plan to fix them and prevent recurrence.

## Audit headlines

| # | Finding | Surface | Severity |
|---|---|---|---|
| A | alint.org docs subtree still pins v0.9.20 (12 lines, 5 files) | Public docs | Critical |
| B | README claims "60 rule kinds" but actual is 70 per audit fixture | Public copy | Critical |
| C | LSP design docs in `v0.10/` but v0.10 scope is now case-study coverage | Internal docs | High |
| D | v0.11 roadmap framing drift between canonical + public | Both | High |
| ~~E~~ | ~~`Cargo.toml workspace.dependencies` pinned to 0.9.8~~ → **audit error**: the 0.9.8 pin is an intentional API-compat floor documented in `RELEASING.md`. C1 reframed to assert `pin <= workspace.package.version` rather than equality. | Build | n/a |
| F | `npm/package.json` version 0.9.8 vs workspace 0.9.21 | Publish | Medium |
| G | ARCHITECTURE.md ~12 days behind v0.9 engine changes | Internal docs | Medium |
| H | Blog post draft has stale v0.9.20 + 60 rule kinds + 12 families | Marketing | Low (draft) |
| I | `docs/development/` + PROPOSAL-AGENTS.md disposition unclear | Internal docs | Low |
| (new) | Canonical ROADMAP.md line 9 still says "Latest release: v0.9.20" | Internal docs | Medium |

Full audit transcript is in the conversation history (2026-05-14).

## Decisions locked in (2026-05-14)

- **Rule-count canonical**: **70 across 13 families** (matches `all_kinds.yaml` audit gate)
- **LSP design docs**: **move from `v0.10/` to `v0.11/`**; rewrite `v0.10/README.md` as case-study coverage scope
- **Public roadmap sync**: **generate from canonical at build time** via section markers (Batch E)
- **Workspace internal-dep versions**: **bump to `workspace.package.version` + preflight check** (lockstep)
- **npm shim version**: **bump to 0.9.21 + lockstep with workspace** every release
- **`docs/development/`**: **keep all + add README** with per-file audience labels
- **PROPOSAL-AGENTS.md**: **refresh as current-state vision doc** (separate session)

## Commit batches

### Batch A — Launch unblockers (~30 min) — DONE 2026-05-14

**Architecture note (discovered during A1 execution):** alint.org's `src/content/docs/docs/{about,bundled-rulesets,changelog,cli,concepts,configuration,cookbook,development,getting-started,integrations,rules}/` is gitignored on alint.org (`.gitignore:28-38`). Canonical sources live in the alint repo at `docs/site/`, `docs/rules.md`, `docs/design/{ROADMAP,ARCHITECTURE}.md`, `CHANGELOG.md`, and `crates/alint-dsl/rulesets/v1/`. The `docs-bundle` orphan branch (force-pushed by `xtask docs-export` on every push to `main`) is what Cloudflare clones at build time. So sweeps targeting synced files MUST edit the alint canonical, not alint.org. See [`reference_alint-org-docs-pipeline.md`](../../../.claude/projects/-home-kaminsod-projects-alint/memory/reference_alint-org-docs-pipeline.md) (memory).

**A1**. `chore: bump v0.9.21 references + v0.11 title sync` (alint canonical sources)
- `docs/design/ROADMAP.md:7` — header version stamp v0.9.20 (2026-05-10) → v0.9.21 (2026-05-14)
- `docs/design/ROADMAP.md:7-17` — body description rewritten to lead with v0.9.21 commit-range mode (#26), preserves v0.9.12-v0.9.20 capstone context in summarized form
- `docs/design/ROADMAP.md:27` — "v0.9.20 is the version we expect to launch on" → "v0.9.21..."
- `docs/design/ROADMAP.md:939` — v0.11 section title already "LSP + DSL polish" (no edit needed; alint.org's synced copy will catch up on next sync)
- `docs/rules.md:823` — `asamarts/alint@v0.9.20` → `@v0.9.21` (range-mode example needs the version that supports it)
- The integrations/ + installation.md files in `docs/site/` were already at v0.9.21 per prior audit. Cloudflare sync pulls these to alint.org on next push to `alint:main`.

**A2**. `docs: update rule-count claim to 70 across 13 families` (alint canonical)
- `README.md:11, 43` — "60 rule kinds across 13 families" → "70 rule kinds across 13 families"
- `docs/site/about/index.md:10` — same correction (about-page narrative on alint.org)
- alint.org's docs/about/index.md will inherit via sync.

**A3**. `docs: refresh non-synced surfaces for v0.9.21 + 70 rule kinds + 13 families` (alint.org)
- `src/content/blog/2026-launch-alint.md:26-27` — v0.9.20 → v0.9.21, 60 → 70, 12 families → 13 families
- `src/content/blog/2026-launch-alint.md:101` — "19 bundled rulesets in v0.9.20" → "v0.9.21"
- `src/content/docs/docs/index.mdx:28, 29, 53` — `v0.9.20` → `v0.9.21`, `60` → `70`, `12 families` → `13 families` (hand-written exception per .gitignore comment)
- `src/pages/index.astro:305` — "60 rule kinds, 13 families" → "70..."
- `src/pages/roadmap.astro:99` — `60` → `70` (header already at v0.9.21)
- `src/pages/language-agnostic-linter.astro:17, 89, 98` — three "60 rule kinds" → "70"
- `src/pages/compare.astro:190, 334` — two "60 rule kinds" → "70"
- `src/pages/llms-full.txt.ts:3` — "60 rule docs" comment → "70 rule docs"
- Keep blog `draft: true` until launch coordination.

**A3-aside (cleanup needed)**: During execution I made wrong-place edits to several alint.org gitignored synced files (`src/content/docs/docs/{about/roadmap.md,about/index.md,integrations/*.md,getting-started/installation.md,rules/index.md}`). These edits are on disk but git won't see them as modified (gitignored). They'll be overwritten by the next `docs-bundle` sync once the alint canonical changes land. Safe to leave; or `npm run sync` in alint.org will overwrite immediately.

**Rule-count discrepancy flagged**: alint.org's `rules/index.md` documents 60 distinct rule kinds (sum of per-family counts: 4+14+7+2+6+3+1+3+2+4+6+7+1). The catalog page's header was bumped to "70 rule kinds across 13 families (60 distinct rule behaviors plus 10 short-name aliases like `content_matches` → `file_content_matches`)" to keep the page internally consistent. If a clean "70" framing is desired across the catalog, ~10 alias entries need separate documentation. Out of scope for v0.9.22; track as a follow-up.

### Batch B — Structural cleanup (~90 min)

**B1**. `docs(design): move LSP design docs from v0.10/ to v0.11/` (alint)
- `git mv docs/design/v0.10/{lsp_server,vscode_extension,single_file_reevaluation}.md docs/design/v0.11/`
- `Cargo.toml:67-74` — comment + path refs updated
- `docs/design/ROADMAP.md:960-966` — update v0.11 forward-refs to new paths
- `docs/design/ROADMAP.md:755-756` — append "(moved to v0.11/ in v0.9.22)" to v0.9.7 design-pass note
- Rewrite `docs/design/v0.10/README.md` to describe case-study coverage scope
- Spot-check moved files for internal cross-refs

**B2**. `chore: align workspace + npm versions with workspace.package.version` (alint)
- `Cargo.toml:26-31` — six internal-dep `version = "0.9.8"` → `"0.9.21"`
- `npm/package.json:3` — `"version": "0.9.8"` → `"0.9.21"`
- `RELEASING.md` — add lockstep convention note

**B3**. `docs(arch): refresh ARCHITECTURE.md for v0.9 engine changes` (alint)
- Add sections for:
  - `PerFileRule` trait + dispatch flip (v0.9.3)
  - Memory layout: `Arc<Path>`, `Arc<str>`, `Cow<'static, str>` (v0.9.2)
  - `FileIndex` lazy indexes: `contains_file`, `children_of`, `descendants_of`, `file_basenames_of` (v0.9.5 + v0.9.8)
  - `Scope` owning `Option<ScopeFilter>` (v0.9.10)
  - Git-tracked filtered index (v0.9.11)
- Fix "Planned additions: v0.10 LSP and v0.11 WASM" off-by-one
- Cross-link to `docs/design/v0.9/*.md`

### Batch C — Prevention infrastructure (~3-4 hrs)

**C1**. `ci(preflight): assert workspace internal-dep + npm versions match workspace.package.version` (alint)
- New script (or fold into existing `check-version-pins.sh`)
- Parses `Cargo.toml`'s `workspace.package.version`; asserts every `path:` entry in `workspace.dependencies` has matching `version =`
- Asserts `npm/package.json`'s version matches
- Wired into `ci/scripts/preflight.sh`

**C2**. `test(rules): assert README claims match registry` (alint)
- New test `crates/alint-rules/tests/readme_claims.rs`
- Parses README for `(\d+) rule kinds across (\d+) families`, `(\d+) bundled .* rulesets`, `(\d+) auto-fix ops`, `(\d+) output formats`, `(\d+) subcommands`
- Computes actuals from registry / fixture / fixers dir / Format enum / Subcommand enum
- Asserts each pair matches

**C3**. `ci: add cross-repo version-pin sync check` (alint.org)
- New `scripts/check-version-pins.sh` invoked from PR/push workflow
- Fetches latest alint release tag via unauthenticated GitHub Releases API
- Greps `src/content/docs/docs/`, `src/pages/`, `src/content/blog/` for `v0\.\d+\.\d+` strings
- Asserts all match latest tag (or are explicitly historical, e.g., changelog/case-study/blog-archive)
- Fails build on drift

**C4**. `docs(release): tag-only recovery + version-lockstep convention` (alint)
- RELEASING.md — v0.9.16-style tag-only recovery paragraph
- Document workspace + npm + workspace.package.version lockstep
- Add release-checklist line: rule-count claim test must pass

### Batch D — Housekeeping

**D1**. `docs(dev): add README explaining docs/development/ contents` (alint)
- New `docs/development/README.md`
- Per-file audience labels:
  - `rule-authoring.md` — public (synced to alint.org)
  - `launch-evidence.md` — public (referenced from ROADMAP + blog)
  - `CONFIG-AUTHORING.md` — TBD on classification
  - `case-study-deep-analysis-log.md` — internal, retained for context
  - `case-study-revalidation-log.md` + `case-study-revalidation-batch-{1..6}.md` — internal
  - `marketing-extraction-batch-{1..6}.md` — internal

**D2**. `docs: refresh PROPOSAL-AGENTS.md as current-state vision doc` (alint, separate session)
- Rewrite end-to-end:
  - v0.6 shipped: `agent-hygiene@v1`, `agent-context@v1`, `--format=agent`
  - v0.7 shipped: `markdown_paths_resolve`, `commented_out_code`, `git_blame_age`, `alint suggest`, `alint export-agents-md`
  - v0.10 case-study coverage as "what's next"
- Add status header at top

### Batch E — Roadmap generator (separate v0.11/v0.12 cycle)

**E1**. `design(v0.11): roadmap generator from canonical ROADMAP.md` (alint)
- New `docs/design/v0.11/roadmap_generator.md`
- Section marker convention: `<!-- alint:internal-start -->` / `<!-- alint:internal-end -->`
- Generator: `xtask gen-public-roadmap` — idempotent, deterministic byte output
- Build wiring: invoke from docs-bundle branch sync
- Migration: mark public roadmap as generated; gitignore on alint.org; first run must byte-match current public (modulo v0.11 title fix from A1)

**E2**. `feat(xtask): roadmap generator + alint.org gitignore + docs-bundle wiring` (alint + alint.org)
- Implementation of E1; cross-repo coordination commit

## Sequencing

| Stage | Batches | Effort | Why this order |
|---|---|---|---|
| 1 | A | ~30 min | Mechanical; unblocks any subsequent publish |
| 2 | B3 | separable | Low risk; independently reviewable |
| 3 | B2 + C1 | ~30 min | Version cleanup + preflight in one pair locks in the fix |
| 4 | C2 | ~90 min | README-claims test locks in A2's correction |
| 5 | C3 | ~60 min | Cross-repo version-pin check locks in A1's correction |
| 6 | B1 + C4 | ~60 min | Design-doc move + RELEASING.md polish |
| 7 | D1 | ~30 min | Opportunistic |
| 8 | D2 | separate session | Strategic writing, not edit work |
| 9 | E1 + E2 | v0.11/v0.12 cycle | Bigger lift; own design pass |

## Status tracker

- [x] **A1** — alint.org v0.9.21 sweep + v0.11 title sync (edits in canonical `docs/design/ROADMAP.md` + `docs/rules.md`; alint.org synced files will refresh via `docs-bundle` pipeline on push to `alint:main`)
- [x] **A2** — alint README rule-count update
- [x] **A3** — alint.org blog refresh (blog + non-synced pages); canonical `docs/site/about/index.md` also bumped for consistency
- [x] **B1** — `git mv lsp_server.md vscode_extension.md single_file_reevaluation.md` from `docs/design/v0.10/` to `docs/design/v0.11/`. Cargo.toml comment + ROADMAP.md v0.11 forward-refs + v0.9.7 historical note + v0.11/README.md table updated. v0.10/README.md rewritten as case-study coverage scope.
- [x] **B2** — Align npm/package.json with workspace version + auto-bump via `bump-version.sh`; RELEASING.md note adjusted. **Cargo.toml `[workspace.dependencies]` floors deliberately NOT touched** (the audit finding was wrong: RELEASING.md documents these as an intentional "API-compat floor", bumped by hand only on inter-crate API breaks).
- [x] **B3** — ARCHITECTURE.md refresh (added PerFileRule + dispatch flip, memory layout, scope_filter v0.9.10 structural fix, git-tracked filtered index v0.9.11, FileIndex lazy indexes in Execution model; fixed v0.10/v0.11 LSP off-by-one in Planned additions; fixed output formats `summary` → `agent`)
- [x] **C1** — New `ci/scripts/check-workspace-dep-floors.sh` asserts each path-having `[workspace.dependencies]` pin is `<= workspace.package.version`. `check-version-pins.sh` extended to check `npm/package.json`. Both wired into `preflight.sh`.
- [x] **C2** — `crates/alint-e2e/tests/coverage_audit_readme_claims.rs`: 6 README count claims (rule kinds, families, bundled rulesets, auto-fix ops, output formats, subcommands) asserted against canonical source per claim. Plus 4 unit tests on the parser helpers. Runs in `cargo test --workspace` so picked up by preflight automatically.
- [x] **C3** — `alint.org/scripts/check-version-pins.sh` fetches the latest alint release tag from GitHub and asserts three install-pin sites (workflow `uses:` pin, JSON-LD `softwareVersion`, roadmap "Latest release" callout) match. Wired into a new `.github/workflows/check-pins.yml` with PR + push + daily cron triggers. Scope deliberately narrow to avoid false-positive flags on historical-reference pages (benchmarks trajectory, comparison, migration guides, etc.).
- [x] **C4** — RELEASING.md step 3 rewritten to call `bash ci/scripts/preflight.sh` (the new 7-gate superset) instead of listing the four-command subset. Mentioned the two recurrence guards inside (README-claims test from C2, workspace-dep-floor check from C1) so contributors know what's behind the curtain. `docs-bundle.yml` row in the workflow table appended with a cross-ref to alint.org's `check-pins.yml`. Earlier B2 commit already covered the workspace.dependencies + npm note adjustments.
- [ ] **D1** — docs/development/README.md
- [ ] **D2** — PROPOSAL-AGENTS.md refresh (separate session)
- [ ] **E1** — Roadmap generator design doc
- [ ] **E2** — Roadmap generator implementation

## Risks + mitigations

- **C2 (rule-count test) may be brittle on README phrasing changes.** Write the regex permissively; test against the actual README in the same PR.
- **B2 version bumps could surface latent semver issues for downstream consumers.** Lands before next `cargo publish`; verify with `cargo publish --dry-run` per crate.
- **C3 cross-repo check needs GitHub Releases API access.** Use unauthenticated public-API endpoint; fall back gracefully on rate limit.
- **B1 `git mv` preserves history but breaks any external link to `v0.10/lsp_server.md`.** Search alint.org content for `v0.10/lsp_server` and add a redirect stub if any references found.

## Out of scope for v0.9.22

- Rust code-quality review (clippy, perf) beyond preflight
- CHANGELOG entry composition for v0.9.22 — written when batches A–D land
- Schema / report-format changes — v0.9.22 is doc + prevention only
- Case-study revalidation runs against the corpus
- Cross-repo migration of `marketing/` artifacts — keep alint.org-only convention

## Audit findings cross-reference

For traceability, each batch maps back to specific findings from the 2026-05-14 audit:

- A1 → Finding A (alint.org v0.9.21 sweep) + Finding D-interim (v0.11 title)
- A2 → Finding B (rule-count claim)
- A3 → Finding H (blog post)
- B1 → Finding C (v0.10/v0.11 design-doc dirs)
- B2 → Findings E + F (Cargo.toml + npm versions)
- B3 → Finding G (ARCHITECTURE.md staleness)
- C1 → Finding E recurrence surface
- C2 → Finding B recurrence surface
- C3 → Finding A recurrence surface
- C4 → Finding I-RELEASING.md polish
- D1 → Finding I-`docs/development/` disposition
- D2 → Finding I-PROPOSAL-AGENTS disposition
- E1+E2 → Finding D recurrence surface
