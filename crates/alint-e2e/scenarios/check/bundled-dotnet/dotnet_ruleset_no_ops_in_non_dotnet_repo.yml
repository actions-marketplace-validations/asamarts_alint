name: alint://bundled/dotnet - every rule is silent when facts.has_dotnet is false
tags: [check, extends, bundled, dotnet]

given:
  tree:
    "README.md": "# not a dotnet project\n"
  config: |
    version: 1
    extends:
      - alint://bundled/dotnet@v1

when: [check]

expect:
  - violations: []
